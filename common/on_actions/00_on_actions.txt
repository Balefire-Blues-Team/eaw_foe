on_actions = {
	on_nuke_drop = {
		
		effect = {
			set_global_flag = first_nuke_dropped
			if = {
				limit = {
					FROM = { state = 21 }
				}
				set_country_flag = achievement_has_nuked_tenpony
			}
		}
	}
	on_state_control_changed = {
		effect = {
			if = {
				limit = {
					FROM.FROM = { state = 51 } #Empire
				}
				ROOT = { add_ideas = EMP_empire_building_state_hidden_idea }
				FROM = { remove_ideas = EMP_empire_building_state_hidden_idea }
			}
		}
	}
}