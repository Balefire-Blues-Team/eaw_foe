STT_phial_project_dix_cat = {
	STT_soldier_take_drugs_dec = {
		icon = GFX_decision_scales
		cost = 100
		ai_will_do = {
			base = 0
			modifier = {
			}
		}

		available = {
			NOT = {
				has_country_flag = STT_soldier_drugs
			}
		}
		visible = {
			has_completed_focus = STT_project_dix
		}
		modifier = {
		}

		days_remove = 0

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision STT_soldier_take_drugs_dec"
			country_event = { id = STT.9 }
		}
	}
	STT_sober_up_dec = {
		icon = GFX_decision_scales
		cost = 100
		ai_will_do = {
			base = 0
			modifier = {
			}
		}

		available = {
			has_country_flag = STT_soldier_drugs
		}
		visible = {
			has_completed_focus = STT_project_dix
			has_country_flag = STT_soldier_drugs
		}
		modifier = {
		}

		days_remove = 70

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision STT_soldier_take_drugs_dec"
			if = {
				limit = {
					has_idea = STT_speedy_sonsofbitches
				}
				remove_ideas = STT_speedy_sonsofbitches
			}
			if = {
				limit = {
					has_idea = STT_hide_armor
				}
				remove_ideas = STT_hide_armor
			}
			if = {
				limit = {
					has_idea = STT_horse_on_meth
				}
				remove_ideas = STT_horse_on_meth
			}
		}
	}
}