COR_NOV_joins_civwar_cat = {
	COR_NOV_joins_civwar = {
		icon = GFX_decision_stable_door
		allowed = { tag = NOV }
		ai_will_do = {
			base = 100
		}
		activation  = {
			has_country_flag = NOV_civwar_join_counter_flag
		}
		fire_only_once = yes
		is_good = no
		selectable_mission = no
		days_mission_timeout = 10

		will_lead_to_war_with = COM
		timeout_effect = {
			declare_war_on = {
				target = COM
				type = annex_everything
			}
			clr_country_flag = NOV_civwar_join_counter_flag
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision COR_NOV_joins_civwar"
			add_political_power = 50
			clr_country_flag = NOV_civwar_join_counter_flag
		}
	}
	COR_DSB_joins_civwar = {
		icon = GFX_decision_stable_door
		allowed = { tag = DSB }
		ai_will_do = {
			base = 100
		}
		activation  = {
			has_country_flag = DSB_civwar_join_counter_flag
		}
		fire_only_once = yes
		is_good = no
		selectable_mission = no
		days_mission_timeout = 10

		will_lead_to_war_with = NOV
		will_lead_to_war_with = COR
		timeout_effect = {
			declare_war_on = {
				target = COR
				type = annex_everything
			}
			declare_war_on = {
				target = NOV
				type = annex_everything
			}
			clr_country_flag = DSB_civwar_join_counter_flag
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision COR_DSB_joins_civwar"
			add_political_power = 50
			clr_country_flag = DSB_civwar_join_counter_flag
		}
	}
	
}
