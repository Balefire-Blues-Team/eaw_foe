COR_NOV_joins_civwar_cat = {
	COR_NOV_joins_civwar = {
		icon = GFX_decision_stable_door
		allowed = { tag = NOV }
		ai_will_do = {
			base = 100
		}
		activation  = {
			has_country_flag = NOV_civwar_join_counter_flag
		}
		available = {
			COM = { exists = no }
		}
		fire_only_once = yes
		is_good = no
		selectable_mission = no
		days_mission_timeout = 10

		will_lead_to_war_with = COM
		timeout_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision COR_NOV_joins_civwar"
			declare_war_on = {
				target = COM
				type = annex_everything
			}
			clr_country_flag = NOV_civwar_join_counter_flag
		}

		complete_effect = {
			add_political_power = 50
			clr_country_flag = NOV_civwar_join_counter_flag
		}
	}
	COR_DSB_joins_civwar = {
		icon = GFX_decision_stable_door
		allowed = { tag = DSB }
		ai_will_do = {
			base = 100
		}
		activation  = {
			has_country_flag = DSB_civwar_join_counter_flag
		}
		available = {
			COR = { exists = no }
		}
		fire_only_once = yes
		is_good = no
		selectable_mission = no
		days_mission_timeout = 10

		will_lead_to_war_with = NOV
		will_lead_to_war_with = COR
		timeout_effect = {
			declare_war_on = {
				target = COR
				type = annex_everything
			}
			declare_war_on = {
				target = NOV
				type = annex_everything
			}
			clr_country_flag = DSB_civwar_join_counter_flag
		}

		complete_effect = {
			log = "[GetDateText]: [Root.GetName]: Decision COR_DSB_joins_civwar"
			add_political_power = 50
			clr_country_flag = DSB_civwar_join_counter_flag
		}
	}
	
}

COR_revolutionary_congress_cat = {
	COR_agitate_survivalism = {
		icon = GFX_decision_stable_door
		allowed = { tag = COR }
		cost = 50
		ai_will_do = {
			base = 0
		}
		visible = {
			NOT = {
				has_completed_focus = COR_victory_at_last
			}
		}
		available = {
			NOT = { has_country_flag = COR_ongoing_propaganda_flag }
			survivalism < 0.95
		}
		fire_only_once = no
		is_good = yes
		days_remove = 50
		days_re_enable = 10

		complete_effect = {
			set_country_flag = COR_ongoing_propaganda_flag
		}
		modifier = {
			survivalism_drift = 0.10
		}
		remove_effect = {
			clr_country_flag = COR_ongoing_propaganda_flag
		}
	}
	COR_agitate_visionary = {
		icon = GFX_decision_stable_door
		allowed = { tag = COR }
		cost = 50
		ai_will_do = {
			base = 0
		}
		visible = {
			NOT = {
				has_completed_focus = COR_victory_at_last
			}
		}
		available = {
			NOT = { has_country_flag = COR_ongoing_propaganda_flag }
			visionary < 0.95
		}
		fire_only_once = no
		is_good = yes
		days_remove = 50
		days_re_enable = 10

		complete_effect = {
			set_country_flag = COR_ongoing_propaganda_flag
		}
		modifier = {
			visionary_drift = 0.10
		}
		remove_effect = {
			clr_country_flag = COR_ongoing_propaganda_flag
		}
	}
	COR_agitate_continuation = {
		icon = GFX_decision_stable_door
		allowed = { tag = COR }
		cost = 50
		ai_will_do = {
			base = 0
		}
		visible = {
			NOT = {
				has_completed_focus = COR_victory_at_last
			}
		}
		available = {
			NOT = { has_country_flag = COR_ongoing_propaganda_flag }
			continuation < 0.95
		}
		fire_only_once = no
		is_good = yes
		days_remove = 50
		days_re_enable = 10

		complete_effect = {
			set_country_flag = COR_ongoing_propaganda_flag
		}
		modifier = {
			continuation_drift = 0.10
		}
		remove_effect = {
			clr_country_flag = COR_ongoing_propaganda_flag
		}
	}
	COR_agitate_destruction = {
		icon = GFX_decision_stable_door
		allowed = { tag = COR }
		cost = 50
		ai_will_do = {
			base = 0
		}
		visible = {
			NOT = {
				has_completed_focus = COR_victory_at_last
			}
		}
		available = {
			NOT = { has_country_flag = COR_ongoing_propaganda_flag }
			destruction < 0.95
		}
		fire_only_once = no
		is_good = yes
		days_remove = 50
		days_re_enable = 10

		complete_effect = {
			set_country_flag = COR_ongoing_propaganda_flag
		}
		modifier = {
			destruction_drift = 0.10
		}
		remove_effect = {
			clr_country_flag = COR_ongoing_propaganda_flag
		}
	}
	COR_hunt_down_remnants = {
		icon = GFX_decision_stable_door
		allowed = { tag = COR }
		cost = 100
		ai_will_do = {
			base = 100
		}
		visible = {
			OR = {
				has_idea = COR_counterrevolutionary_activity_1
				has_idea = COR_counterrevolutionary_activity_2
				has_idea = COR_counterrevolutionary_activity_3
				has_idea = COR_counterrevolutionary_activity_4
				has_idea = COR_counterrevolutionary_activity_5
			}
		}
		available = {
			has_army_manpower = {
				size > 8000
			}
			has_war = no
		}
		fire_only_once = no
		is_good = yes
		days_remove = 50
		days_re_enable = 100

		modifier = {
			conscription_factor = -0.10
		}
		remove_effect = {
			COR_lower_counterrev = yes
		}
	}
	COR_invite_SYT_congress = {
		icon = GFX_decision_stable_door
		allowed = { tag = COR }
		cost = 20
		ai_will_do = {
			base = 100
		}
		visible = {
			SYT = {
				exists = yes 
				is_subject = no
			}
			has_completed_focus = COR_found_pan_equestian_congress
		}
		available = {
			any_neighbor_country = {
				tag = SYT
			}
		}
		fire_only_once = yes
		is_good = yes

		complete_effect = {
			SYT = { country_event = vermillionists.47 }
		}
	}
	COR_invite_NVR_congress = {
		icon = GFX_decision_stable_door
		allowed = { tag = COR }
		cost = 20
		ai_will_do = {
			base = 100
		}
		visible = {
			NVR = {
				exists = yes 
				is_subject = no
			}
			has_completed_focus = COR_found_pan_equestian_congress
			has_country_flag = COR_NVR_aligned_flag
		}
		available = {
			any_neighbor_country = {
				tag = NVR
			}
		}
		fire_only_once = yes
		is_good = yes

		complete_effect = {
			NVR = { country_event = vermillionists.47 }
		}
	}
	COR_kill_NVR = {
		icon = GFX_decision_stable_door
		allowed = { tag = COR }
		cost = 20
		ai_will_do = {
			base = 100
		}
		visible = {
			NVR = {
				exists = yes 
				is_subject = no
			}
			has_completed_focus = COR_found_pan_equestian_congress
			has_country_flag = COR_NVR_enemies_flag
		}
		available = {
			any_neighbor_country = {
				tag = NVR
			}
		}
		fire_only_once = yes
		is_good = yes

		complete_effect = {
			NVR = {
				ROOT = {
					grant_wargoals_on_core_states_of_prev = yes
				}
			}
		}
	}
	COR_trade_TEN_congress = {
		icon = GFX_decision_stable_door
		allowed = { tag = COR }
		cost = 20
		ai_will_do = {
			base = 100
		}
		visible = {
			TEN = {
				exists = yes 
				is_subject = no
			}
			has_country_flag = COR_trade_with_man_flag
		}
		available = {
			NOT = {
				TEN = {
					has_government = destruction
				}
			}
		}
		fire_only_once = yes
		is_good = yes

		complete_effect = {
			TEN = { country_event = vermillionists.69 }
		}
	}
	COR_trade_ABT_congress = {
		icon = GFX_decision_stable_door
		allowed = { tag = COR }
		cost = 20
		ai_will_do = {
			base = 100
		}
		visible = {
			ABT = {
				exists = yes 
				is_subject = no
			}
			has_country_flag = COR_trade_with_man_flag
		}
		available = {
			NOT = {
				ABT = {
					has_government = destruction
				}
			}
		}
		fire_only_once = yes
		is_good = yes

		complete_effect = {
			ABT = { country_event = vermillionists.69 }
		}
	}
	COR_trade_EMP_congress = {
		icon = GFX_decision_stable_door
		allowed = { tag = COR }
		cost = 20
		ai_will_do = {
			base = 100
		}
		visible = {
			EMP = {
				exists = yes 
				is_subject = no
			}
			has_country_flag = COR_trade_with_man_flag
		}
		available = {
			NOT = {
				EMP = {
					has_government = destruction
				}
			}
		}
		fire_only_once = yes
		is_good = yes

		complete_effect = {
			EMP = { country_event = vermillionists.69 }
		}
	}
	COR_trade_MSR_congress = {
		icon = GFX_decision_stable_door
		allowed = { tag = COR }
		cost = 20
		ai_will_do = {
			base = 100
		}
		visible = {
			MSR = {
				exists = yes 
				is_subject = no
			}
			has_country_flag = COR_trade_with_man_flag
		}
		available = {
			NOT = {
				MSR = {
					has_government = destruction
				}
			}
		}
		fire_only_once = yes
		is_good = yes

		complete_effect = {
			MSR = { country_event = vermillionists.69 }
		}
	}
	COR_trade_FRI_congress = {
		icon = GFX_decision_stable_door
		allowed = { tag = COR }
		cost = 20
		ai_will_do = {
			base = 100
		}
		visible = {
			FRI = {
				exists = yes 
				is_subject = no
			}
			has_country_flag = COR_trade_with_man_flag
		}
		available = {
			NOT = {
				FRI = {
					has_government = destruction
				}
			}
		}
		fire_only_once = yes
		is_good = yes

		complete_effect = {
			FRI = { country_event = vermillionists.69 }
		}
	}
	COR_manehattan_republic_congress = {
		icon = GFX_decision_stable_door
		allowed = { tag = COR }
		cost = 50
		ai_will_do = {
			base = 100
		}
		visible = {
			has_completed_focus = COR_our_approach_manehattan
		}
		available = {
			EMP = { all_core_land_owned_by_prev_sphere = yes }
			TEN = { all_core_land_owned_by_prev_sphere = yes }
			FRI = { all_core_land_owned_by_prev_sphere = yes }
			ABT = { all_core_land_owned_by_prev_sphere = yes }
			MSR = { all_core_land_owned_by_prev_sphere = yes }
		}
		fire_only_once = yes
		is_good = yes

		complete_effect = {
			every_state = {
				limit = {
					OR = {
						is_core_of = EMP
						is_core_of = ABT
						is_core_of = MSR
						is_core_of = ARB
						is_core_of = FRI
						is_core_of = TEN
						is_core_of = GUT
						is_core_of = MRA
						is_core_of = RAR
						is_core_of = MAR
					}
				}
				add_core_of = EPC
			}
			release_puppet = EPC
			EPC = {
				inherit_technology = COR
				set_capital = {
					state = 21
					remember_old_capital = no
				}
				set_cosmetic_tag = COR_manehattan_puppet_cosm
			}
			IF = {
				limit = {
					OR = {
						EMP = { is_subject_of = COR }
						EMP = { is_in_faction_with = COR }
					}
				}
				EPC = {
					annex_country = {
						target = EMP
						transfer_troops = yes
					}
				}
			}
			IF = {
				limit = {
					OR = {
						MSR = { is_subject_of = COR }
						MSR = { is_in_faction_with = COR }
					}
				}
				EPC = {
					annex_country = {
						target = MSR
						transfer_troops = yes
					}
				}
			}
			IF = {
				limit = {
					OR = {
						ABT = { is_subject_of = COR }
						ABT = { is_in_faction_with = COR }
					}
				}
				EPC = {
					annex_country = {
						target = ABT
						transfer_troops = yes
					}
				}
			}
			IF = {
				limit = {
					OR = {
						ARB = { is_subject_of = COR }
						ARB = { is_in_faction_with = COR }
					}
				}
				EPC = {
					annex_country = {
						target = ARB
						transfer_troops = yes
					}
				}
			}
			IF = {
				limit = {
					OR = {
						MRA = { is_subject_of = COR }
						MRA = { is_in_faction_with = COR }
					}
				}
				EPC = {
					annex_country = {
						target = MRA
						transfer_troops = yes
					}
				}
			}
			IF = {
				limit = {
					OR = {
						TEN = { is_subject_of = COR }
						TEN = { is_in_faction_with = COR }
					}
				}
				EPC = {
					annex_country = {
						target = TEN
						transfer_troops = yes
					}
				}
			}
			IF = {
				limit = {
					OR = {
						FRI = { is_subject_of = COR }
						FRI = { is_in_faction_with = COR }
					}
				}
				EPC = {
					annex_country = {
						target = FRI
						transfer_troops = yes
					}
				}
			}
		}
	}

}
