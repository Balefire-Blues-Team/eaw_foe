
# ZZZ faction template
ZZZ_hidden_faction = {
	name = ZZZ_hidden_faction
	#manifest = faction_manifest_strength_in_unity
	icon = GFX_faction_icon_ZZZ

	visible = {
		always = no
	}
	
	goals = {
		# Short term
	
		# Medium term
		
		# Long term
	}

	default_rules = {
		faction_peace_take_claimed_focus
	}

}

# Fallback template for factions raised by script that carry their own name,
# e.g. the Greenway Compact in events/shatteredhoof_events.txt. Not offered to
# players, so it stays invisible and leaves the peace rules at the default.
faction_template_generic = {
	name = "Alliance"
	manifest = faction_manifest_strength_in_unity
	icon = GFX_faction_logo_generic

	visible = {
		always = no
	}

	goals = {
		# Short term

		# Medium term

		# Long term
	}

	default_rules = {
		faction_peace_rule_conquest_focus
	}
}
