add_namespace = mango

country_event = { #Money Generator #1
	id = mango.1
	immediate = {log = "[GetDateText]: [Root.GetName]: event mango.1"}
	is_triggered_only = yes
	hidden = yes

	option = {
		name = mango.1.a
		set_temp_variable = { 
		    caps_to_add = 150 
		}
        add_caps = yes
		if = {
		    limit = {
			    has_idea = MAN_gateway
			}
		    country_event = { 
				id = mango.1
                days = 92 				
			}
		}
	}
}

country_event = { #Money Generator #2
	id = mango.2
	immediate = {log = "[GetDateText]: [Root.GetName]: event mango.2"}
	is_triggered_only = yes
	hidden = yes

	option = {
		name = mango.2.a
		set_temp_variable = { 
		    caps_to_add = 50 
		}
        add_caps = yes
		if = {
		    limit = {
			    OR = {
			        has_idea = MAN_moonlight
					has_country_leader = { 
					    ruling_only = yes 
						name = "Moonlight Poppy" 
					}
				}
			}
		    country_event = { 
				id = mango.2
                days = 92 				
			}
		}
	}
}