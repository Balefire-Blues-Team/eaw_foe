add_namespace = goldendawnrevolt

country_event = {
	id = goldendawnrevolt.1
	title = goldendawnrevolt.1.t
	desc = goldendawnrevolt.1.d

	is_triggered_only = yes
 	fire_only_once = no

 	option = {
		name = goldendawnrevolt.1.a
		ai_chance = { factor = 1000}
		add_manpower = 1000
		add_offsite_building = {
			type = arms_factory
			level = 2
		}
	}
}

country_event = {
	id = goldendawnrevolt.2
	title = goldendawnrevolt.2.t
	desc = goldendawnrevolt.2.d

	is_triggered_only = yes
 	fire_only_once = no

 	option = {
		name = goldendawnrevolt.2.a
		ai_chance = { factor = 1000}
		
	}
}

country_event = {
	id = goldendawnrevolt.3
	title = goldendawnrevolt.3.t
	desc = goldendawnrevolt.3.d

	is_triggered_only = yes
 	fire_only_once = no

 	option = {
		name = goldendawnrevolt.3.a
		ai_chance = { factor = 1000}
		remove_ideas = TMS_GDS_glory_of_liberation_idea
		swap_ideas = {
			remove_idea = TMS_GDS_mercs_idea2
			add_idea = TMS_GDS_reconstruction_idea
		}
		leave_faction = yes
		mark_focus_tree_layout_dirty = yes
		hidden_effect = {
			BMR = {
				every_owned_state = {
					limit = {
						NOT = { is_core_of = BMR }
					}
					add_compliance = 20
					add_resistance = -50
				}
			}
			SLC = {
				every_owned_state = {
					limit = {
						NOT = { is_core_of = SLC }
					}
					add_compliance = 20
					add_resistance = -50
				}
			}
			LAE = {
				every_owned_state = {
					limit = {
						NOT = { is_core_of = LAE }
					}
					add_compliance = 20
					add_resistance = -50
				}
			}
		}
	}
}